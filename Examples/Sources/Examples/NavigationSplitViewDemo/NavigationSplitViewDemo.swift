/// define the basic structure and namespaces
enum NavigationSplitViewDemo {
    enum List { 
        enum Effects {} 
        enum Views {}
    }
    enum Detail { 
        enum Effects {} 
        enum Views {}
    }
    
    enum Main {
        enum Effects {}
        enum Views {}
    }
}
